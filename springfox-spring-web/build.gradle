dependencies {
  compile libs.core
  compile libs.spring
  compile project(':springfox-spi')
  compile project(':springfox-schema').sourceSets.main.output

    compileOnly libs.springProvided
    compileOnly libs.clientProvided
    compileOnly "org.reflections:reflections:$reflections"
}
